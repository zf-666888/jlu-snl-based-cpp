# 编译原理课程设计实验报告

## 一、实验目的

通过对 SNL（Small Nested Language）编译程序的设计与实现，深入掌握编译程序的工作原理和实现技术，包括词法分析、语法分析和语义分析三大核心模块。同时培养大型软件的程序设计方法和模块化编程能力。

## 二、实验内容

### 2.1 SNL 语言简介

SNL（Small Nested Language）是一种自行定义的教学模型语言，类似 Pascal 的"高级"程序设计语言。其主要特点：
- 数据类型：整型（integer）、字符型（char）、数组（array）、记录（record）
- 过程允许嵌套定义，允许递归调用

### 2.2 SNL 程序结构示例

```
program pp
var   integer  v1;
      char  c;
procedure f();
begin
  v1 := 2
end
begin
  f();
  write(v1)
end.
```

### 2.3 SNL 文法描述

SNL 的上下文无关文法共 104 条产生式，涵盖：
- 程序结构：Program → ProgramHead DeclarePart ProgramBody .
- 类型声明：TypeDec → TYPE TypeDecList | ε
- 变量声明：VarDec → VAR VarDecList | ε
- 过程声明：ProcDec → PROCEDURE ProcName ( ParamList ) ; ProcDecPart ProcBody ProcDecMore | ε
- 语句：Stm → ConditionalStm | LoopStm | InputStm | OutputStm | ReturnStm | ID AssCall
- 表达式：Exp → Term OtherTerm，支持 +、-、*、/、<、= 运算符

## 三、总体设计

### 3.1 模块划分

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  词法分析器  │───▶│  语法分析器  │───▶│  语义分析器  │
│  (scan)      │    │  (parse)     │    │  (analyze)   │
└─────────────┘    └─────────────┘    └─────────────┘
       │                  │                   │
       ▼                  ▼                   ▼
   Token 序列        语法树             符号表+错误信息
```

### 3.2 文件结构

| 文件 | 功能 |
|------|------|
| globals.h | 全局定义（Token 类型、节点类型、符号表结构等） |
| scan.h / scan.cpp | 词法分析器 |
| tree.h / tree.cpp | 语法树节点定义与打印 |
| parse.h / parse.cpp | 语法分析器（递归下降） |
| analyze.h / analyze.cpp | 语义分析器（符号表 + 类型检查） |
| main.cpp | 主程序入口 |

### 3.3 核心数据结构

**Token 结构：**
```cpp
struct Token {
    int lineNo;      // 行号
    LexType lex;     // 单词类别
    string sem;      // 语义信息
};
```

**语法树节点：**
```cpp
struct TreeNode {
    TreeNode* child[4];    // 子节点
    TreeNode* sibling;     // 兄弟节点
    int lineno;
    NodeKind nodekind;     // ProK, PheadK, TypeK, VarK, ProcDecK, DecK, StmLk, StmtK, ExpK
    StmtKind stmtkind;     // IfK, WhileK, AssignK, ReadK, WriteK, CallK, ReturnK
    ExpKind expkind;       // OpK, ConstK, IdV
    LexType op;
    int val;
    string name, type_name;
};
```

**符号表条目：**
```cpp
struct Symbol {
    string name;
    IdKind kind;           // TypeIdK, VarIdK, ProcIdK
    string type_name;
    int level;             // 嵌套层次
    vector<FieldItem> fields;   // 记录字段
    vector<ParamItem> params;   // 过程参数
};
```

## 四、详细设计

### 4.1 词法分析器

**设计思路：** 基于 DFA 的状态转移驱动，逐字符读取源程序，识别单词并生成 Token 序列。

**核心算法：**
1. 跳过空白符和注释（`{...}` 花括号注释）
2. 以字母开头 → 读取标识符，查保留字表判断是否为保留字
3. 以数字开头 → 读取整数常量
4. 以单引号开头 → 读取字符常量
5. 特殊字符 → 识别运算符和分隔符（`:=`、`..` 等双字符运算符需向前查看）

**保留字表（21 个）：** program, if, then, else, end, procedure, type, var, while, array, of, record, integer, char, read, write, return, endwh, begin, fi, do

**Token 类别（共 28 种）：**
- 保留字：PROGRAM, IF, THEN, ELSE, END, PROCEDURE, TYPE, VAR, WHILE, ARRAY, OF, RECORD, INTEGER, CHAR, READ, WRITE, RETURN, ENDWH, BEGIN, FI, DO
- 标识符/常量：ID, INTC, CHARC
- 运算符：PLUS, MINUS, TIMES, OVER, LT, EQ, ASSIGN
- 分隔符：LPAREN, RPAREN, SEMI, COMMA, DOT, UNDERANGE, LBRACE, RBRACE
- 特殊：ENDFILE

### 4.2 语法分析器

**设计思路：** 采用递归下降分析法，为每个非终结符编写一个分析函数，通过向前查看（lookahead）一个 Token 来决定使用哪个产生式。

**核心函数对应关系：**

| 非终结符 | 分析函数 | 文法规则 |
|---------|---------|---------|
| Program | program() | 规则 1-3 |
| DeclarePart | declarePart() | 规则 4 |
| TypeDec | typeDec() | 规则 6-10 |
| VarDec | varDec() | 规则 30-35 |
| ProcDec | procDec() | 规则 39-43 |
| ProgramBody | programBody() | 规则 57 |
| StmList | stmList() | 规则 58-60 |
| Stm | statement() | 规则 61-66 |
| ConditionalStm | conditionalStm() | 规则 70 |
| LoopStm | loopStm() | 规则 71 |
| Exp | exp() | 规则 83-85 |
| Term | term() | 规则 86-88 |
| Factor | factor() | 规则 89-91 |
| Variable | variable() | 规则 92-95 |

**错误处理：** 使用同步符号集进行错误恢复。遇到语法错误时，输出行号和错误描述，跳过输入直到遇到同步符号。

### 4.3 语法树

**节点类型（按 PPT 定义）：**
- ProK：程序根节点
- PheadK：程序头
- TypeK：类型声明列表头
- VarK：变量声明列表头
- ProcDecK：过程声明节点
- DecK：声明节点
- StmLk：语句列表头
- StmtK：语句节点（子类型：IfK, WhileK, AssignK, ReadK, WriteK, CallK, ReturnK）
- ExpK：表达式节点（子类型：OpK, ConstK, IdV）

**树的输出：** 采用层次文本输出，缩进表示父子关系。

### 4.4 语义分析器

**设计思路：** 遍历语法树，声明部分建立符号表，语句部分查表检查语义。

**符号表管理：**
- 使用作用域栈（`vector<map<string, Symbol>>`）实现嵌套作用域
- 进入新作用域时压栈，退出时弹栈
- 查找标识符时从内层作用域向外层逐层查找

**实现的语义检查（12 种）：**

| 编号 | 错误类型 | 检测方式 |
|-----|---------|---------|
| 1 | 标识符重复定义 | 插入符号表时检查 |
| 2 | 无声明的标识符 | 查表失败时报错 |
| 3 | 标识符类别不匹配 | 查表后检查 kind |
| 4 | 数组下标越界 | 编译期检查常量下标 |
| 5 | 数组/域变量引用不合法 | 检查类型是否为数组/记录 |
| 6 | 赋值语句类型不兼容 | 比较左右两侧类型 |
| 7 | 赋值左端不是变量 | 检查左值的 kind |
| 8 | 形实参类型不匹配 | 逐个比较参数类型 |
| 9 | 形实参个数不匹配 | 比较参数数量 |
| 10 | 调用非过程标识符 | 检查标识符的 kind |
| 11 | if/while 条件非布尔 | 检查条件表达式类型 |
| 12 | 运算符分量类型不兼容 | 检查操作数类型 |

**类型兼容性：** integer 和 char 之间允许互操作。

## 五、测试结果

### 5.1 正确测试用例

| 编号 | 文件名 | 描述 | 结果 |
|-----|--------|------|------|
| 1 | t1_basic.snl | 简单赋值和 write | 通过 |
| 2 | t2_arithmetic.snl | 算术表达式 | 通过 |
| 3 | t3_if.snl | if-then-else-fi | 通过 |
| 4 | t4_while.snl | while 循环求和 | 通过 |
| 5 | t5_array.snl | 数组声明和访问 | 通过 |
| 6 | t6_record.snl | 记录类型 | 通过 |
| 7 | t7_proc.snl | 过程声明和调用 | 通过 |
| 8 | t8_nested.snl | 嵌套过程 | 通过 |
| 9 | t9_recursive.snl | 递归过程（阶乘） | 通过 |
| 10 | t10_full.snl | 综合测试 | 通过 |

所有正确测试用例均无语法错误和语义错误。

### 5.2 错误测试用例

| 编号 | 文件名 | 错误类型 | 检测结果 |
|-----|--------|---------|---------|
| 1 | e1_lexer.snl | 非法字符 '@' | 词法错误 + 语法错误 |
| 2 | e2_undef_id.snl | 未声明标识符 y | 语义错误（2处） |
| 3 | e3_dup_decl.snl | 重复定义变量 x | 语义错误（1处） |
| 4 | e4_type_mismatch.snl | 赋值类型不匹配 | 词法/语法错误 |
| 5 | e5_param_count.snl | 形实参数量不匹配 | 语义错误（1处） |
| 6 | e6_param_type.snl | 形实参类型不匹配 | 词法/语法错误 |
| 7 | e7_not_var.snl | 赋值左端非变量 | 语义错误（2处） |
| 8 | e8_not_proc.snl | 调用非过程标识符 | 语义错误（1处） |
| 9 | e9_array_bound.snl | 数组类型引用错误 | 语义错误（1处） |
| 10 | e10_cond_type.snl | 条件类型不兼容 | 语义错误（1处） |

### 5.3 测试示例输出

**正确用例 t1_basic.snl 的词法分析输出：**
```
(2, PROGRAM, "program")
(2, ID, "t1")
(3, VAR, "var")
(3, INTEGER, "integer")
(3, ID, "v1")
(3, SEMI, ";")
(4, BEGIN, "begin")
(5, ID, "v1")
(5, ASSIGN, ":=")
(5, INTC, "10")
(5, SEMI, ";")
(6, WRITE, "write")
(6, LPAREN, "(")
(6, ID, "v1")
(6, RPAREN, ")")
(7, END, "end")
(7, DOT, ".")
(8, ENDFILE, "EOF")
```

**正确用例 t4_while.snl 的语法树输出：**
```
ProK
  PheadK  t4
  VarK
    DecK  integer  i
    DecK  integer  sum
  StmLk
    StmtK  Assign
      ExpK  sum  IdV
      ExpK  Const  0
    StmtK  Assign
      ExpK  i  IdV
      ExpK  Const  1
    StmtK  While
      ExpK  Op  <
        ExpK  i  IdV
        ExpK  Const  11
      StmtK  Assign
        ExpK  sum  IdV
        ExpK  Op  +
          ExpK  sum  IdV
          ExpK  i  IdV
      StmtK  Assign
        ExpK  i  IdV
        ExpK  Op  +
          ExpK  i  IdV
          ExpK  Const  1
    StmtK  Write
      ExpK  sum  IdV
```

**错误用例 e5_param_count.snl 的错误输出：**
```
Line 8: Semantic Error - 过程调用形实参个数不相同: 期望 2，实际 1
```

## 六、总结与心得

通过本次编译原理课程设计，我完成了 SNL 语言编译器的词法分析、语法分析和语义分析三个核心模块的设计与实现。

**主要收获：**
1. 深入理解了编译器的工作原理，将理论知识转化为实际代码
2. 掌握了递归下降语法分析法的实现技巧，包括 predict 集的使用和错误恢复
3. 学会了符号表的设计与管理，特别是嵌套作用域的实现
4. 培养了模块化编程和大型软件设计的能力

**遇到的困难及解决：**
1. 文法规则的左递归处理：通过改写文法消除左递归
2. 错误恢复机制：采用同步符号集进行错误恢复
3. 嵌套作用域管理：使用作用域栈实现从内到外的符号查找
4. 自定义类型解析：通过类型定义表将自定义类型名解析为基础类型

**改进方向：**
1. 可以实现更完善的错误恢复机制，支持更多的错误场景
2. 可以添加目标代码生成模块（MIPS 汇编）
3. 可以支持更多的数据类型和运算符
